---
description: AAF Sample 项目升级规则 - 自动同步示例项目的 AAF 版本和编译配置
alwaysApply: false
enabled: true
updatedAt: 2025-11-28T08:00:00.000Z
provider: 
---

# AAF Sample 项目升级规则

## 📖 规则说明

本规则用于自动升级 Template_Android 和 Template-AAF 示例项目的 AAF 框架版本和编译配置，确保示例项目与 AAF 主项目保持同步。

**项目说明**：
- **Template-Empty** - 提供给外部开发者的最简示例（空壳项目，快速开始）
- **Template_Android** - 提供给外部开发者的基础 Android 项目示例
- **Template-AAF** - 提供给外部开发者的 AAF 框架完整示例
- **作用** - 展示 AAF 框架的最佳实践和标准用法
- **注意** - 这些都是外部示例项目，不直接在其中开发
- **概念区分** - Sample vs Demo 的详细说明参见 `aaf_common.mdc`

---

## 🎯 触发条件

当用户说出以下关键词时，自动执行本规则：
- "升级sample" / "升级Sample"
- "升级demo" / "升级Demo"
- "更新sample版本" / "更新demo版本"
- "同步sample配置"
- "升级示例项目"
- "更新Template"

**说明**：无论说"升级demo"还是"升级sample"，都是升级外部示例项目（Template-Empty/Template_Android/Template-AAF），因为这些是提供给外部开发者的标准示例。AAF-Temp 是内部临时开发项目，不需要专门升级。

---

## 📂 涉及项目和文件

### 1. AndroidAppFactory（配置源）
- **文件**: `config.gradle`
  - 读取 SDK 版本配置：`compileSdkVersion`, `buildToolsVersion`, `libMinSdkVersion`, `targetSdkVersion`
- **文件**: `dependencies.gradle`
  - 读取 AAF 通用版本号：`ext.moduleVersionName`
- **文件**: `dependencies_*.gradle` (所有依赖配置文件)
  - 读取各模块的具体版本：通过 `artifactId` 匹配查找对应的 `version`
  - 示例：`common-debug` → 在 `dependencies_common.gradle` 中查找 `"artifactId" : "common-debug"` 对应的 `"version"`

### 2. Template-Empty（更新目标）
- **文件**: `config.gradle`
  - 更新：`compileSdkVersion`, `buildToolsVersion`, `appMinSdkVersion`, `targetSdkVersion`
- **文件**: `App/build.gradle`
  - 更新：所有 `com.bihe0832.android:xxx:x.x.x` 依赖版本

### 3. Template_Android（更新目标）
- **文件**: `config.gradle`
  - 更新：`compileSdkVersion`, `buildToolsVersion`, `libMinSdkVersion`, `targetSdkVersion`
- **文件**: `Application/build.gradle`
  - 更新：所有 `com.bihe0832.android:xxx:x.x.x` 依赖版本
- **文件**: `APPTest/build.gradle`
  - 更新：所有 `com.bihe0832.android:xxx:x.x.x` 依赖版本

### 4. Template-AAF（更新目标）
- **文件**: `config.gradle`
  - 更新：`compileSdkVersion`, `buildToolsVersion`, `libMinSdkVersion`, `targetSdkVersion`
- **文件**: `dependencies.gradle`
  - 更新：所有 `com.bihe0832.android:xxx:x.x.x` 依赖版本

---

## ⚙️ 执行流程

### 步骤 1: 读取 AAF 最新配置

```bash
# 1.1 读取 AAF 通用版本号
cd /Volumes/Document/Documents/github/AndroidAppFactory
AAF_VERSION=$(grep "ext.moduleVersionName" dependencies.gradle | grep -o '".*"' | tr -d '"')

# 1.2 读取 SDK 配置
COMPILE_SDK=$(grep "compileSdkVersion = " config.gradle | grep -o '[0-9]*')
BUILD_TOOLS=$(grep "buildToolsVersion = " config.gradle | grep -o '".*"' | tr -d '"')
LIB_MIN_SDK=$(grep "libMinSdkVersion = " config.gradle | grep -o '[0-9]*')
TARGET_SDK=$(grep "targetSdkVersion = " config.gradle | grep -o '[0-9]*')

# 1.3 读取各模块的具体版本 (通过 artifactId 查找)
# 重要：不是所有模块都发布到 ext.moduleVersionName，需要根据 artifactId 在对应的 dependencies_*.gradle 中查找实际版本
# 
# 查找方法：
# - 在 AAF 的 dependencies_*.gradle 文件中搜索 "artifactId" : "模块名"
# - 读取对应的 "version" 字段
# 
# 示例：
# common-debug → 在 dependencies_common.gradle 中查找
#   "CommonDebug" : [
#       "version"    : "8.0.0",
#       "artifactId" : "common-debug",
#       ...
#   ]
# 
# lib-router-compiler → 在 dependencies_lib.gradle 中查找
#   "RouterCompiler" : [
#       "version"    : "5.1.7",
#       "artifactId" : "lib-router-compiler",
#       ...
#   ]
```

**版本查找策略**：
1. **优先使用模块实际版本**：在 `dependencies_*.gradle` 中通过 `artifactId` 查找对应的 `version`
2. **备选通用版本**：如果找不到，使用 `ext.moduleVersionName` 作为默认版本
3. **Maven 验证**：对于关键依赖（如 kapt 处理器），验证 Maven 仓库中是否已发布该版本
4. **版本回退**：如果 Maven 未发布，使用已发布的最新版本

### 步骤 2: 更新 Template-Empty

```bash
cd /Volumes/Document/Documents/github/Template-Empty

# 2.1 更新 config.gradle
# - compileSdkVersion
# - buildToolsVersion
# - appMinSdkVersion (对应 AAF 的 libMinSdkVersion)
# - targetSdkVersion

# 2.2 更新 App/build.gradle
# 依赖更新方法：
# - 提取 artifactId (如 common-compose-debug)
# - 在 AAF/dependencies_common.gradle 中查找对应的 version
# - 如果未找到，使用 ext.moduleVersionName
# 
# 示例依赖及其版本来源：
# - common-compose-debug → dependencies_common.gradle 的 CommonDebugCompose.version
# - common-wrapper-min → dependencies_common.gradle 的 CommonWrapperMin.version
# - lib-router-compiler → dependencies_lib.gradle 的 RouterCompiler.version (注意：可能未发布到最新版)

# 2.3 兼容性修复 (Android 12+ 要求)
# - 检查 AndroidManifest.xml 中是否需要添加 android:exported
# - 创建 libs/ 目录 (如果不存在)
```

### 步骤 3: 更新 Template_Android

```bash
cd /Volumes/Document/Documents/github/Template_Android

# 3.1 更新 config.gradle
# - compileSdkVersion
# - buildToolsVersion
# - libMinSdkVersion
# - targetSdkVersion

# 3.2 更新 Application/build.gradle
# 依赖更新：
# - common-wrapper → dependencies_common.gradle 的 CommonWrapper.version
# - lib-router-compiler → dependencies_lib.gradle 的 RouterCompiler.version

# 3.3 更新 APPTest/build.gradle (重要！容易遗漏)
# 依赖更新：
# - common-debug → dependencies_common.gradle 的 CommonDebug.version
# - lib-router-compiler → dependencies_lib.gradle 的 RouterCompiler.version
```

### 步骤 4: 更新 Template-AAF

```bash
cd /Volumes/Document/Documents/github/Template-AAF

# 4.1 更新 config.gradle
# - compileSdkVersion
# - buildToolsVersion
# - libMinSdkVersion
# - targetSdkVersion

# 4.2 更新 dependencies.gradle
# 两种更新方式：
# 
# 方式 1：直接更新 ext.moduleVersionName
# - ext.moduleVersionName 作为默认版本
# 
# 方式 2：逐个更新模块依赖版本
# - 在 AAF/dependencies_*.gradle 中通过 artifactId 查找实际版本
# - 示例：
#   "Application": [
#       "apidependenciesList": [
#           "com.bihe0832.android:common-wrapper:x.x.x"  → 查找 common-wrapper 的实际版本
#       ],
#       "specialdependenciesList": [
#           "kapt": ["com.bihe0832.android:lib-router-compiler:x.x.x"]  → 查找 lib-router-compiler 的实际版本
#       ]
#   ]
```

### 步骤 4.5: 版本号查找

**重要**：不是所有 AAF 模块都发布到 `ext.moduleVersionName`，需要根据 `artifactId` 查找实际版本。

**详细的版本查找方法请参考**: `aaf_version.mdc` 规则

**快速参考**：
- ✅ 优先：从 `dependencies_*.gradle` 中通过 `artifactId` 查找模块实际版本
- ✅ 备选：使用 `ext.moduleVersionName` 作为默认版本
- ✅ 验证：检查 Maven 仓库是否已发布该版本
- ⚠️ 特殊：`lib-router-compiler` 等编译器模块通常独立更新，版本可能不同

输出格式：
```
✅ AAF 最新配置
   版本号: 8.0.0
   compileSdkVersion: 34
   buildToolsVersion: 34.0.0
   libMinSdkVersion: 23
   targetSdkVersion: 31

📦 Template-Empty（最简示例）变更
   config.gradle:
   - compileSdkVersion: 32 → 34
   - buildToolsVersion: 32.0.0 → 34.0.0
   - appMinSdkVersion: 16 → 23
   - targetSdkVersion: 30 → 31
   
   Application/build.gradle:
   - common-compose-debug: 7.1.1 → 8.0.0
   - lib-router-compiler: 5.1.7 → 8.0.0
   - common-wrapper-min: 7.1.1 → 8.0.0

📦 Template_Android（基础示例）变更
   config.gradle:
   - compileSdkVersion: 32 → 34
   - buildToolsVersion: 32.0.0 → 34.0.0
   - libMinSdkVersion: 16 → 23
   - targetSdkVersion: 30 → 31
   
   Application/build.gradle:
   - common-wrapper: 6.21.9 → 8.0.0
   - lib-router-compiler: 5.1.7 → 8.0.0

📦 Template-AAF（完整示例）变更
   config.gradle:
   - compileSdkVersion: 32 → 34
   - buildToolsVersion: 32.0.0 → 34.0.0
   - libMinSdkVersion: 16 → 23
   - targetSdkVersion: 30 → 31
   
   dependencies.gradle:
   - common-wrapper: 6.21.9 → 8.0.0
   - common-debug: 6.21.12 → 8.0.0
   - lib-router-compiler: 5.1.7 → 8.0.0

🔍 编译验证
   ✅ Template-Empty: 编译通过
   ✅ Template_Android: 编译通过
   ✅ Template-AAF: 编译通过
```

### 步骤 5: 验证编译

**重要**：升级后必须验证所有项目编译通过

```bash
# 5.1 验证 Template-Empty 编译
cd /Volumes/Document/Documents/github/Template-Empty
./gradlew clean
./gradlew assembleDebug
# 如果编译失败，记录错误信息并修复

# 5.2 验证 Template_Android 编译
cd /Volumes/Document/Documents/github/Template_Android
./gradlew clean
./gradlew assembleDebug
# 如果编译失败，记录错误信息并修复

# 5.3 验证 Template-AAF 编译
cd /Volumes/Document/Documents/github/Template-AAF
./gradlew clean
./gradlew assembleDebug
# 如果编译失败，记录错误信息并修复
```

**编译验证要求**：
- ✅ 必须执行 `./gradlew clean` 清理旧的编译产物
- ✅ 必须执行 `./gradlew assembleDebug` 验证编译
- ✅ 所有项目都必须编译成功才算升级完成
- ❌ 如果有编译错误，必须先修复再继续

### 步骤 6: 提供提交建议

**只有在所有项目编译通过后才提交**

```bash
# Template-Empty 提交
cd /Volumes/Document/Documents/github/Template-Empty
git add config.gradle Application/build.gradle
git commit -m "chore(sample): 升级 AAF 到 8.0.0 并同步编译配置

- 升级 AAF 依赖到 8.0.0
- 同步编译配置（SDK 34, minSdk 23, targetSdk 31）
- 验证编译通过
"

# Template_Android 提交
cd /Volumes/Document/Documents/github/Template_Android
git add config.gradle Application/build.gradle
git commit -m "chore(sample): 升级 AAF 到 8.0.0 并同步编译配置

- 升级 AAF 依赖到 8.0.0
- 同步编译配置（SDK 34, minSdk 23, targetSdk 31）
- 验证编译通过
"

# Template-AAF 提交
cd /Volumes/Document/Documents/github/Template-AAF
git add config.gradle dependencies.gradle
git commit -m "chore(sample): 升级 AAF 到 8.0.0 并同步编译配置

- 升级 AAF 依赖到 8.0.0
- 同步编译配置（SDK 34, minSdk 23, targetSdk 31）
- 验证编译通过
"
```

---

## 🔍 需要同步的配置项

### config.gradle 配置项

| 配置项 | 说明 | AAF 当前值 |
|--------|------|-----------|
| `compileSdkVersion` | Android 编译 SDK 版本 | 34 |
| `buildToolsVersion` | Android 编译工具版本 | 34.0.0 |
| `libMinSdkVersion` | Lib 最低适配 Android 版本 | 23 |
| `targetSdkVersion` | Android 最佳适配版本 | 31 |

**注意**：以下配置不需要同步（保持 Template 项目原有值）
- `versionCode` - Demo 项目版本号
- `versionName` - Demo 项目版本名
- `signingConfigs` - 签名配置（使用 debug.keystore）
- `kotlin_version` - Kotlin 版本（可选同步）

### dependencies 依赖版本

所有 `com.bihe0832.android:xxx` 开头的依赖都需要更新到 AAF 最新版本。

**常见依赖**：
- `com.bihe0832.android:common-wrapper`
- `com.bihe0832.android:common-debug`
- `com.bihe0832.android:lib-router-compiler`
- 其他所有 AAF 框架依赖

---

## ⚠️ 注意事项

### 1. 签名配置保持不变
Template 项目使用 `debug.keystore`，不要修改为 AAF 的签名配置。

### 2. 版本号不同步
示例项目的 `versionCode` 和 `versionName` 保持独立，不随 AAF 版本更新。

### 3. 检查兼容性
升级 SDK 版本后，可能需要检查：
- Gradle 版本兼容性
- Kotlin 版本兼容性
- 依赖库的兼容性

### 4. 编译验证必须
升级后必须验证所有示例项目编译通过，编译失败必须先修复再提交。

### 5. 分别提交
三个示例项目应该分别提交，不要合并提交。

### 6. 项目升级重点
- **Template-Empty** - 最简示例，空壳项目，帮助开发者快速开始
- **Template_Android** - 基础示例，展示标准 Android 集成 AAF 的方式
- **Template-AAF** - 完整示例，展示 AAF 框架的完整功能和最佳实践
- **共同要求**：保持最新版本，展示最佳实践，供外部开发者参考
- **重要说明**：这3个都是外部示例项目，不直接在其中开发
- **概念说明**：Sample vs Demo 的详细区分参见 `aaf_common.mdc` 第 1.2 节

---

## 📝 执行清单

执行升级时，确认以下步骤：

- [ ] 读取 AAF 最新版本号 (ext.moduleVersionName)
- [ ] 读取 AAF SDK 配置
- [ ] 根据 artifactId 查找各模块的实际版本
- [ ] 更新 Template-Empty/config.gradle
- [ ] 更新 Template-Empty/App/build.gradle
- [ ] 更新 Template_Android/config.gradle
- [ ] 更新 Template_Android/Application/build.gradle
- [ ] **更新 Template_Android/APPTest/build.gradle** ⚠️ **容易遗漏**
- [ ] 更新 Template-AAF/config.gradle
- [ ] 更新 Template-AAF/dependencies.gradle
- [ ] **验证 Template-Empty 编译通过** ⚠️ **必须**
- [ ] **验证 Template_Android 编译通过** ⚠️ **必须**
- [ ] **验证 Template-AAF 编译通过** ⚠️ **必须**
- [ ] 生成变更报告
- [ ] 提供提交建议
- [ ] 确认所有变更无误

---

## 🔗 相关规则

- `aaf_commands.mdc` - 命令索引
- `aaf_common.mdc` - AAF 框架通用规范
- `aaf_version.mdc` - AAF 模块版本查找方法
- `aaf_dependency.mdc` - 依赖管理规范
